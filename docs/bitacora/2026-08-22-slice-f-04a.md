# Slice F-04a — CIMIENTOS VISUALES

> Bitácora escrita a mano, con hora real. Va antes de Municipal (F-05): sus 74 mockups multiplican
> por cinco la superficie a revisar, así que el catálogo de íconos y la geometría se arreglan ahora
> o se clonan 74 veces.
>
> Orden de los pasos: `f-01` circuito chico → `f-02` la red visual → `f-03` refactor visual con la
> red puesta → `f-04` el catálogo. **`f-02` va antes que `f-03` a propósito**, por el mismo
> principio que puso el test antes del refactor en F-03.

| Hora | Paso | Evento | Detalle |
|---|---|---|---|
| 14:00 | f-00 | INICIO | Línea de base medida antes de tocar nada: **479 tests / 35 archivos**, `eslint-suppressions.json` con **13** entradas, **173 stories** en **37** archivos `.stories.tsx`, árbol de git **limpio** (0 pendientes, F-00..F-03 commiteados). |
| 14:01 | f-00 | CONFIRMADO | **Seis de los siete números del prompt dan exactos**: lucide-react en **126** archivos, `<Icono` en **35**, supresiones **13**, tests **479/35**, stories **173**, `zodResolver` en **30** (el número que F-02 corrigió de "~9" a 30). `src/shared/ui/iconos/catalogo.ts` **no existe** y `src/shared/forms/` está **vacía**: las dos premisas del slice se confirman en disco. |
| 14:02 | f-00 | HALLAZGO | **La geometría son 19 ocurrencias, no 14.** Los **8 archivos** coinciden; el conteo no. La lista del prompt clasifica 14 (5 tipo A + 6 tipo B + 3 tipo C) y **omite 5**: `switch.tsx:26 translate-x-[calc(100%-2px)]` (×2), `tooltip.tsx:57 translate-y-[calc(-50%-2px)]`, `tabs.tsx:30 p-[3px]` y `tabs.tsx:67 bottom-[-5px]`. Es el modo de falla de F-02 otra vez: el inventario se armó con lo que se vio, no con lo que se midió. Las 5 se clasifican en f-03 como cualquier otra. |
| 14:12 | f-01 | CONFIRMADO | `src/shared/forms/aplicar-errores-de-campo.ts` escrito. Reusa `resolveApiFieldErrors` (no la reescribe) y exporta `TranslationFn` desde `parse-api-error.ts` en vez de duplicar el tipo. **16 archivos** hacen hoy este bloque a mano, no 30: los 30 son los que usan `zodResolver`; los que repiten el `onError` son 7 páginas de `access/` + 9 archivos de Flota. |
| 14:14 | f-01 | DESVIACION | **Devuelve `number`, no `void` como pide el prompt.** Es una extensión compatible —quien la trata como `void` compila igual— y hace expresable lo que `LoginPage` intentaba con `if (Object.keys(fieldErrors).length > 0)`. Queda declarado, no escondido: si el PO prefiere `void`, es borrar dos líneas. |
| 14:18 | f-01 | CONFIRMADO | 5 tests. Se testea con un **formulario falso**: el proyecto `unit` corre en `environment: 'node'`, y la función toca del form exactamente `setError`, así que un espía prueba el contrato entero sin montar React. |
| 14:22 | f-01 | CORRECCION | **Volví a caer en el ancla vacía.** El arnés de mutación revertía con `''` como ancla y `count('')` da 2633: se comió la línea del `tipo`. Restaurada anclando en la línea vecina e insertando, y el arnés ahora **rechaza el ancla vacía** con un assert. Es el mismo error de F-03, cometido por segunda vez. |
| 14:24 | f-01 | CONFIRMADO | **5 mutaciones, 5 rojos**: devolver solo el primer campo (la que pide el prompt) → 1 · ignorar `tipo` → 1 · poner `tipo` siempre → 2 · no traducir → 3 · no normalizar el nombre del campo → 3. Todas revertidas por edición, `git diff` vacío. |
| 14:30 | f-01 | HALLAZGO | **El test importaba `axios` y F10 lo rechaza** (*"el HTTP vive centralizado en services/"*). No se suprime: el repo **ya lo resolvió** en `errores-alta-conductor.test.ts` — `axios.isAxiosError` solo chequea `isAxiosError === true`, así que el error se fabrica como objeto plano. Se sigue ese precedente y se cita en el docblock. |
| 14:33 | f-01 | PUERTA-VERDE | 3 pilotos migrados (`LoginPage` de access · `ModalEditarVehiculo` y `ModalEditarConductor` de Flota). TC=0 LINT=0 TEST **484/36** BUILD=0 SB=0. Quedan **13** consumidores por migrar. |
| 14:40 | f-02 | CONFIRMADO | Estado del prompt verificado: `chromatic` en `package.json` (script :23 + devDep :66), `@chromatic-com/storybook` como addon en `.storybook/main.ts:11`, y **0 menciones** en `.github/workflows/ci.yml`. La herramienta está instalada y no corre nunca. |
| 14:46 | f-02 | CONFIRMADO | Job `regresion-visual` agregado, **separado** de `verificacion`: manda a un servicio externo y depende de un secret, y si fallara adentro tiraría abajo tipos/lint/tests por una causa ajena. Lleva `fetch-depth: 0` (sin historial Chromatic no encuentra la línea base y trata toda story como nueva) y **sin** `--exit-zero-on-changes`: un cambio visual no aprobado tiene que poner el job en rojo. |
| 14:48 | f-02 | HALLAZGO | **Saqué `--only-changed` antes de que fuera una trampa.** TurboSnap necesita que el build de Storybook emita `stats.json`, que este proyecto no genera; activarlo sin eso rompe el job o cae al snapshot completo en silencio. 173 stories es barato: comparación completa. |
| 14:50 | f-02 | PARADA | **El secret `CHROMATIC_PROJECT_TOKEN` no se pudo verificar: `gh` no está instalado en esta máquina.** No se inventa ni se hardcodea. El job **skipea con `::warning` visible** y un step llamado "SIN COBERTURA VISUAL — falta el secret", para que el verde no mienta. Lo configura el PO en Settings → Secrets and variables → Actions. **Documentado en el job qué detecta y qué no**: no ve pantallas sin story —hoy `features/` y `modules/` no tienen ninguna, o sea las 16 pantallas reales quedan afuera— ni composiciones. |
| 14:52 | f-03 | HALLAZGO | **f-03 se quedó sin la red que f-02 tenía que darle.** En vez de avanzar a ciegas o frenar el slice, se cambió el método de verificación por uno más fuerte: **probar que el cambio es aritméticamente neutro**. |
| 14:55 | f-03 | CONFIRMADO | **El techo era un no-op, y medido.** `--s-radio-md` = `0.625rem` = **10px** (`semanticas.css:132`), así que el techo de 10px y el de 12px **rendían los dos 10px**: la distinción entre los dos valores del prompt no existía en pantalla. `rounded-md` da exactamente lo mismo. |
| 14:57 | f-03 | HALLAZGO | **`min()` ≠ la clase base del botón.** La base es `rounded-lg` = `--s-radio-lg` = 14px; los 4 tamaños chicos la bajaban al capeado. O sea el techo **sí cumplía una función** —hacer los controles chicos menos redondeados— y por eso el reemplazo correcto es `rounded-md` (10px), no borrar la clase. Si lo hubiera borrado, los botones chicos habrían saltado de 10 a 14px. |
| 15:02 | f-03 | CONFIRMADO | **Prueba empírica sobre el CSS que se envía**, no sobre una captura: el bundle emite `.rounded-md{border-radius:var(--s-radio-md)}` con `--s-radio-md:.625rem`, y `border-radius:min(` aparece **0 veces**. Diff visual = cero, demostrado. |
| 15:05 | f-03 | HALLAZGO | **El `18.4px` del switch NO compensa nada: es `1.15rem`.** 1.15 × 16 = 18.4 exacto — es el `h-[1.15rem]` del upstream de shadcn re-expresado en px. Deja ~1.2px de aire alrededor del pulgar de 16px. **No se movió a 18px**: ese cambio no gana tokenización (los dos son números a mano) y sí toca el aire sin que nadie lo pida. Queda con su origen escrito. |
| 15:08 | f-03 | HALLAZGO | **Tres TIPO B no se pudieron arreglar: el token que necesitan NO EXISTE.** `checkbox` pide 4px y `tooltip` (la flecha) 2px; la escala semántica **arranca en `--s-radio-sm` = 8px**. Sobre un checkbox de 16px, 4→8px duplica el redondeo: sería un cambio de diseño disfrazado de refactor. La regla del repo es explícita — *"si necesitás un token que no existe, no lo inventes en el componente: pedilo"* (`frontend-core.md` §1). Quedan como `PENDIENTE (DA-FE-xx)` en el encabezado de cada archivo. |
| 15:10 | f-03 | CONFIRMADO | **TIPO B sí arreglado (3 de 6)**: `w-[32px]`→`w-8`, `w-[24px]`→`w-6`, `h-[14px]`→`h-3.5` en `switch.tsx`. Exactos al píxel (8×4=32, 6×4=24, 3.5×4=14) y **ganan tokenización de verdad**: la escala de espaciado de Tailwind sale de `--spacing: var(--s-unidad-espaciado)` (`base.css:183`). |
| 15:12 | f-03 | CONFIRMADO | **TIPO C intactos, con su razón escrita en el archivo.** Los 3 del prompt (`dialog` ancho máximo, `tabs` corrección de 1px, `dropdown-menu` ancho mínimo) **más los 5 que el prompt no listó**: `tabs` padding de 3px (es DENSIDAD, y el slice declara que no la tokeniza) y desplazamiento de −5px del subrayado, `tooltip` el `calc()` que pega la flecha al globo, y los 2 `translate` del pulgar del switch. Ocho, no tres. |
| 15:15 | f-03 | CORRECCION | **Dos roturas mías, encontradas por la puerta.** (1) El encabezado de desvío **reemplazó** la línea `const buttonVariants = cva(` en vez de anteponerse: typecheck en rojo, restaurado. (2) Mis comentarios **citaban el literal viejo**, así que el criterio de cierre `grep rounded-[min(` daba 2 en vez de 0 — la documentación derrotaba al verificador. Reescritos sin sintaxis de clase arbitraria. |
| 15:18 | f-03 | HALLAZGO | **Volví falso un comentario de `semanticas.css` y lo corregí.** Decía que *"los componentes de shadcn leen la escala de radios dentro de una clase arbitraria"*, y después de este paso **ningún código lo hace** (verificado: 0 hits de `var(--radius-` en `.ts`/`.tsx`). Las declaraciones planas **se quedan** y no son código muerto: `shadcn add --overwrite` vuelve a traer el patrón. Son la red de esa re-copia, y ahora el comentario lo dice. |
| 15:22 | f-03 | CONFIRMADO | Guardián permanente `geometria-tokenizada.test.ts` (3 tests): ningún archivo de `base/` puede capear un token. **3 mutaciones**: reintroducir el techo → rojo · capear otro token que no sea radio → rojo · el patrón dentro de un comentario → **verde**, que es lo correcto (el encabezado de desvío habla del patrón viejo y no debe disparar). Revertidas por edición. |
| 15:24 | f-03 | PUERTA-VERDE | TC=0 LINT=0 TEST **487/37** BUILD=0 SB=0. `grep -rn "rounded-\[min(" src/` → **0** en todo el repo. |
| 15:30 | f-04 | CONFIRMADO | **Coteja contra el mockup: los tres números del doc dan exactos.** `theme-switcher.js` tiene `ICONS` con **60** claves, `TEXT_ICON` con **67** y `ICONSETS` con **11** bibliotecas (`auto, lucide, material, fontawesome, bootstrap, heroicons, feather, tabler, phosphor, remix, ionicons`). El doc no exagera. |
| 15:34 | f-04 | CONFIRMADO | **Los 98 nombres canónicos existen en la lucide instalada** (1.7), verificado importando el paquete, no leyendo la doc. Y **los 8 alias que R-IC-3 prohíbe también existen** (`AlertTriangle`, `CheckCircle`, `CircleCheckBig`, `LinkIcon`, `MoreVertical`, `MoreHorizontal`, `Loader2`, `Filter`): son importables, por eso la regla hace falta. |
| 15:40 | f-04 | CONFIRMADO | `catalogo.ts` con **102 conceptos** (33 entidades + 35 acciones + 20 estados + 14 de navegación). Las filas con dos glifos se parten en dos conceptos (`vincular`/`desvincular`, `pausar`/`reanudar`…) porque el catálogo es 1:1 concepto→glifo. |
| 15:42 | f-04 | HALLAZGO | **Una fila del doc se contradice consigo misma.** En §4, *licencia de conducir* tiene columna "Ícono" = `ShieldCheck` y columna "Estado" = *"`ShieldCheck` queda para seguridad; licencia pasa a `IdCard`"*. Se portó **`IdCard`**, que es el veredicto: dejar `ShieldCheck` haría que dos conceptos compartieran glifo **en el mismo menú de acciones**, que es exactamente el defecto que esa fila reporta. Drift a corregir en la columna "Ícono". |
| 15:44 | f-04 | CONFIRMADO | Lo que **no** entró, a propósito: `activo`/`inactivo` y `en_linea`/`desconectado`/`sin_dato` (son `Badge` con punto — darles glifo empeora la accesibilidad, y está escrito en `Badge.tsx`), el vacío `sin-datos` (usa el ícono de la ENTIDAD que falta), `activo urbano` de Municipal (sale por tipo, de una tabla de catálogo en DB) y `OctagonX` (§6 lo elimina). |
| 15:48 | f-04 | CONFIRMADO | `Icono` acepta `nombre` **sin romper los 35 usos**: unión discriminada, el compilador exige exactamente una de las dos props. El docblock de `Icono.tsx` decía que `nombre` necesitaba *"un registro curado que todavía nadie definió"* — ese párrafo se reescribió, porque ya está definido. |
| 15:52 | f-04 | CONFIRMADO | 12 tests. **5 mutaciones, 5 rojos**: devolver un ícono por defecto en silencio (la del prompt) → 2 · error sin decir dónde está el catálogo → 1 · el módulo Flota compartiendo glifo con la entidad vehículo → 1 · licencia volviendo a `ShieldCheck` → 1 · importar un alias prohibido → 1. |
| 15:58 | f-04 | HALLAZGO | **La regla de lint casi apaga F8, F9 y F10.** `no-restricted-imports` está declarada en **5** bloques y en flat config **gana el último que matchea**: un bloque suelto para lucide habría desactivado las fronteras en las carpetas que matchea. Se metió `LUCIDE` **dentro** de los 5 bloques y la exención (`iconos/` + `base/`) va al final repitiendo `RED` y las patterns de `shared/`. **Verificado con 5 archivos sonda**: F8, F9 y F10 siguen disparando, y lucide queda exento solo en las dos carpetas. |
| 16:02 | f-04 | HALLAZGO | **El impacto real es 111 archivos / 251 violaciones, no ~91.** El prompt estimó `126 − 35`, y esa resta no vale: un archivo usa `<Icono>` **e** importa el glifo para pasárselo — son conjuntos que se solapan, no complementarios. |
| 16:06 | f-04 | CONFIRMADO | **Baseline congelado y verificado como red, no como alfombra.** Pasó de **13 archivos / 25 supresiones** a **122 / 276** (+251, todas R-IC-2). Tres pruebas: lint verde con el baseline · un archivo NUEVO con lucide crudo → **rojo** · **una violación de más en un archivo ya suprimido → rojo** (el baseline cuenta, no marca el archivo entero). |
| 16:12 | f-04 | CONFIRMADO | **6 primitivas compartidas migradas** a la API por concepto — las de mayor palanca, porque las usan todas las pantallas: `Aviso`, `BarraMasiva`, `Enlace`, `EstadoError`, `SinAccesoOverlay` y el `iconoIzq` de `EstadoError`. Dos de ellas usaban `AlertTriangle`, el alias que R-IC-3 prohíbe: se arreglan las dos reglas de un movimiento. Tras podar, el baseline bajó a **117 archivos / 270 supresiones**. |
| 16:15 | f-04 | HALLAZGO | **Faltan DOS conceptos en §4–§7, y aparecieron al intentar usar el catálogo.** (1) **`Paginacion.tsx`**: `ChevronLeft`/`ChevronRight` significan *página anterior / siguiente*, y la única fila con esos glifos es *"colapsar / expandir panel"* — otro concepto. (2) **`Stepper.tsx`**: `Minus` marca un *paso salteado*, y §6 solo tiene `paso-completado`. No se inventan: van por §9. Los dos archivos quedan sin migrar y suprimidos. |
| 16:18 | f-04 | HALLAZGO | **`BotonIcono` sigue tomando el componente.** El ejemplo del doc (§3.2) lo muestra con `nombre`, pero el prompt de este slice pide ampliar `Icono`, no `BotonIcono` — y el propio doc lo tiene como pendiente #11, propiedad de `02-primitivas.md`. Para esos casos el patrón es `icono={ICONOS.cerrar}`: usa el catálogo y no importa lucide. |
| 16:22 | f-04 | PARADA | **`npm run test:storybook` no se pudo correr**: Playwright no tiene el navegador instalado en esta máquina (`Executable doesn't exist ... chrome-headless-shell.exe`). Es previo y ajeno al slice, y ese comando **no está en la puerta**. `npm run build-storybook` —que sí compila las 173 stories— pasa. No se instaló el navegador: es una descarga de ~150 MB y queda del lado del PO. |
| 16:25 | CIERRE | PUERTA-VERDE | TC=0 · LINT=0 (tokens e i18n OK) · TEST **499/38** (baseline 479/35, **+20**) · BUILD=0 · SB=0. `eslint-suppressions.json`: **117 archivos / 270 supresiones**, creció **solo** por R-IC-2. 173 stories en 37 archivos, sin cambios. |
| 16:32 | CIERRE | CONFIRMADO | **Verificación en vivo** (`npm run dev`, sin backend): la app monta, **0 errores de consola**, 21 SVG renderizados. Sondeado desde el navegador: el catálogo expone **102 conceptos**, `iconoDe('no-existe')` **tira** (no devuelve un default), `--s-radio-md` = `0.625rem` y los radios computados de los botones son 14px y 18px — los dos de la escala, ninguno de un techo. |

